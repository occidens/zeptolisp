# zeptolisp

Zeptolisp is yet another LOSS (Lisp of small size).

Currently it is so small that all it does is parse S-Expressions!

To run the REPL (well, the RPL for now):

```
corepack enable # if needed
yarn install
yarn zeptolisp
```
(Requires Node >=22.18.0 to run TS directly)
